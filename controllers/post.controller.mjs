export function index(request, response) {
  const posts = 'Posts string';
  response.send(posts);
}

export function post(request, response) {
  const posts = 'Posts string';
  response.send(posts);
}
